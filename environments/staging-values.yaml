replicaCount: 2

image:
  tag: "1.0.0"

environment: staging

ingress:
  enabled: false
  host: argocd-demo-staging.local

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi
