apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "argocd-demo.fullname" . }}
  labels:
    {{- include "argocd-demo.labels" . | nindent 4 }}
data:
  ENVIRONMENT: {{ .Values.environment | quote }}
